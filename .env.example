# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/ingestion

# Target API Configuration
API_BASE_URL=http://datasync-dev-alb-101078500.us-east-1.elb.amazonaws.com/api/v1
TARGET_API_KEY=ds_a099b469c349291c229372e8f7710f06
# Note: Your API key is valid for 90 minutes from first use
# Note: Header-based auth (X-API-Key) is preferred over query param auth

# Runtime Configuration
DEFAULT_PAGE_LIMIT=5000
LEASE_DURATION_SECONDS=120
WORKER_POLL_DELAY_MS=250

# Retry + Rate Limit Configuration
MAX_TRANSIENT_RETRIES=3
RETRY_BASE_DELAY_MS=250
RETRY_MAX_DELAY_MS=5000

# Export + Submission
EXPORT_FILE_PATH=/app/output/event_ids.txt
EXPORT_BATCH_SIZE=5000
GITHUB_REPO_URL=https://github.com/xavier1of1/data-sync-ingestion-coding-challenge

